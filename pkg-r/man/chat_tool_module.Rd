% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat_module.R
\name{chat_tool_module}
\alias{chat_tool_module}
\title{Serve a Shiny module as an interactive chat tool}
\usage{
chat_tool_module(
  module_ui,
  module_server,
  name,
  description,
  arguments = list(),
  ...,
  title = name,
  open = TRUE
)
}
\arguments{
\item{module_ui}{A Shiny module UI function. Must accept \code{id} as its first
argument and return Shiny UI (tags).}

\item{module_server}{A Shiny module server function. Must accept \code{id} as its
first argument.}

\item{name}{The tool name that the LLM sees. Should be a short,
descriptive identifier (e.g., \code{"scatter_plot"}, \code{"data_filter"}).}

\item{description}{A description of what this module does, used by the LLM
to decide when to call the tool.}

\item{arguments}{A named list of \link[ellmer:ellmer-package]{ellmer::ellmer} type definitions for parameters
the LLM should provide when calling the tool. These are passed to
\code{module_server} as additional arguments after \code{id}. Defaults to an empty
list (no LLM-provided arguments).}

\item{...}{Additional arguments passed to \code{module_server} after \code{id} (and
after any LLM-provided arguments from \code{arguments}). Use this to pass
shared reactive values, datasets, or other session state.}

\item{title}{A display title for the tool result card shown in the chat.
Defaults to \code{name}.}

\item{open}{Whether the tool result card should be expanded by default.
Defaults to \code{TRUE}.}
}
\value{
An \code{\link[ellmer:tool]{ellmer::tool()}} object suitable for passing to a chat client's
\code{tools} argument.
}
\description{
\code{chat_tool_module()} registers a Shiny module as an \code{\link[ellmer:tool]{ellmer::tool()}} that can
be called by an LLM during a chat conversation. When the LLM invokes the
tool, the module's UI is rendered directly in the chat as fully interactive
content — inputs, outputs, and all — connected to the module's server
function running in the current Shiny session.

This allows you to build chat applications where the LLM can summon rich,
interactive UI components on demand: data visualizations the user can
interact with, forms that write back to session state, tables with
filtering, and more.
}
\section{How it works}{


When the LLM decides to call the tool:
\enumerate{
\item A unique namespaced ID is generated for the module instance.
\item The module server function is called, connecting it to the Shiny session.
\item The module UI is rendered and returned as the tool result.
\item shinychat inserts the HTML into the chat and calls
\code{Shiny.initializeInputs()} + \code{Shiny.bindAll()}, making the module fully
interactive.
}

Because the module server runs in the current session, it has full access to
shared reactive values, other session state, and can communicate with other
modules.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(shiny)
library(bslib)
library(dowshinychat)
library(ellmer)

# Define a simple module
hist_ui <- function(id) {
  ns <- NS(id)
  plotOutput(ns("plot"), height = "250px")
}

hist_server <- function(id, n_bins) {
  moduleServer(id, function(input, output, session) {
    output$plot <- renderPlot({
      hist(faithful$eruptions, breaks = n_bins)
    })
  })
}

# Register as a chat tool
hist_tool <- chat_tool_module(
  module_ui = hist_ui,
  module_server = hist_server,
  name = "histogram",
  description = "Show a histogram of Old Faithful eruption times.",
  arguments = list(
    n_bins = ellmer::type_integer("Number of bins for the histogram")
  )
)

ui <- page_fillable(
  chat_ui("chat", fill = TRUE)
)

server <- function(input, output, session) {
  chat <- chat_openai(
    system_prompt = "Help the user explore data. Use tools when appropriate.",
    tools = list(hist_tool)
  )

  observeEvent(input$chat_user_input, {
    stream <- chat$stream_async(input$chat_user_input)
    chat_append("chat", stream)
  })
}

shinyApp(ui, server)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=chat_append_module]{chat_append_module()}} for programmatically inserting modules
without LLM involvement.
}
